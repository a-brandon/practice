def is_parsel_tongue(sentence):
    sentence = [word for word in sentence.lower().split() if 's' in word]
    for word in sentence:
        if word[word.index('s') + 1] != 's':
            return False
    return True
